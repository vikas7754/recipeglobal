export const metadata = {
  title: "RecipeGlobal - Sign Up",
  description: "Sign Up to RecipeGlobal to access your account and more",
};

export default function RootLayout({ children }) {
  return children;
}
